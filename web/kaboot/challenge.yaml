name: kaboot
author: kpdfgo
description: |
  off-brand companies is my passion

  [Instancer](https://instancer.tjctf.org/challenge/kaboot)
flag:
  file: server/flag.txt

provide:
  - kind: zip
    spec:
      as: server.zip
      exclude:
        - server/flag.txt
      files:
        - server
      additional:
        - path: server/flag.txt
          str: flag{fake_flag}

